<% @posts.each do |post| %>
  <div>
    <h3><strong><%= post.title %></strong></h3>
    <p><strong>Author</strong>: <%= link_to post.user.name, user_path(post.user) %></p>
    <%= link_to "Open", post %>
  </div>
  <hr>
<% end %>
<p><%= link_to "New Post", new_post_path %></p>
